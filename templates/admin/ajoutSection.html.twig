<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Ajout Section{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
	<center>	
	<h3>Ajout Section</br></h3>
{{ form_start(form) }}
{{ form_widget(form) }}</center>
{{ form_end(form) }}